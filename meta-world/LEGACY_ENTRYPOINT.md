# Legacy directory compatibility

This directory now mirrors the corrected `metaworld/` runtime. Its prior
seven-task setup is selectable as `--task-suites mw_legacy7`; old notebooks may
have explicit obsolete flags and must not be used to resume old checkpoints.
The paper task-list option is `mw_paper10`, not an exact baseline reproduction.
