numpy>=1.26,<3
torch>=2.1
gymnasium>=1.0,<2
stable-baselines3>=2.4,<3
tensorboard>=2.15
matplotlib>=3.7
tyro>=0.8
loguru>=0.7
tqdm>=4.66
scipy>=1.11
# NOTE: mujoco and metaworld are deliberately NOT listed here.
# run_kaggle.sh installs mujoco first, then Meta-World at a pinned commit with
# --no-deps, then filters these two names out of this file before installing
# the rest. That keeps pip from resolving Meta-World's stale dependency pins
# and downgrading torch/numpy/gymnasium underneath the run.
